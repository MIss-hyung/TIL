* model을 이용하여 View에 데이터 넘겨주기
    @controller 클래스 안에 @RequestMapping어노테이션이 부여된 method들은 사용자에게 응답할 view를 생성하는 역할을 하는 죽, controller 메소드이다.
    controller는 model을 이용해 데이터를 가져오고 view에 데이터를 넘겨 적절한 view를 생성하는 역할을 한다.

    controller에서 사용자에게 응답할 view를 생성할 때 model을 이용하여 view에 데이터를 전달하는 방법을 알아보겠다.

