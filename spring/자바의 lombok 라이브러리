자바의 Lombok 라이브러리
C#은 get, set 자동 구현 프로퍼티를 제공, 
java는 lombok이라는 라이브러리를 추가하여 어노테이션으로 getter setter를 변수에 처리 가능

- gradle wrapper(줄여서 gradlew)는 새로운 환경에서 프로젝트를 설정할 때 java나 gradle을 설치하지 않고 바로 빌드할 수 있게 해주는 역할을 한다.
